<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Register – Personal Blog</title>
  <style>
    body { font-family: 'Comic Neue', Arial, sans-serif; background: #fafafa; }
    .card {
      border: 2px solid #444;
      border-radius: 10px;
      padding: 2.5em;
      margin: 5em auto;
      max-width: 370px;
      text-align: center;
    }
    h2 { margin-top: 0; }
    label { display: block; margin-top: 1em; text-align: left; }
    input { width: 100%; font-size: 1em; border-radius: 5px; border: 1px solid #aaa; padding: 0.4em;}
    button { margin-top: 1.5em; padding: 0.6em 1.2em; background: #4a76fd; color: #fff; border: none; border-radius: 6px; font-weight: bold;}
    .error { color: #d22; margin-bottom: 1em; }
    .alt-link { margin-top: 1.3em; display: block;}
    a { color: #4a76fd; text-decoration: none;}
  </style>
</head>
<body>
  <div class="card">
    <h2>Register</h2>
    <% if (typeof error !== "undefined") { %>
      <div class="error"><%= error %></div>
    <% } %>
    <form method="POST" action="/register">
      <label>
        Username
        <input type="text" name="username" required>
      </label>
      <label>
        Password
        <input type="password" name="password" required>
      </label>
      <label>
        Confirm Password
        <input type="password" name="confirmPassword" required>
      </label>
      <button type="submit">Register</button>
    </form>
    <div class="alt-link">
      <span>Already have an account? <a href="/login">Login</a></span>
    </div>
    <div class="alt-link">
      <a href="/home">← Back to Home</a>
    </div>
  </div>
</body>
</html>
