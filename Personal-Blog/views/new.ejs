<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>New Article – Admin</title>
  <style>
    body { font-family: 'Comic Neue', Arial, sans-serif; background: #fafafa; }
    .card { border: 2px solid #444; border-radius: 10px; padding: 2em; margin: 2em auto; max-width: 450px;}
    h2 { margin-top: 0; }
    label { display: block; margin-top: 1em; }
    input, textarea { width: 100%; font-size: 1em; border-radius: 5px; border: 1px solid #aaa; padding: 0.4em;}
    textarea { min-height: 120px; }
    button { margin-top: 1em; padding: 0.6em 1.2em; background: #444; color: #fff; border: none; border-radius: 6px; font-weight: bold;}
    .error { color: #d22; margin-bottom: 1em; }
  </style>
</head>
<body>
  <div class="card">
    <h2>New Article</h2>
    <% if (typeof error !== "undefined") { %>
      <div class="error"><%= error %></div>
    <% } %>
    <form method="POST" action="/admin/new">
      <label>Article Title
        <input type="text" name="title" value="<%= typeof title !== 'undefined' ? title : '' %>" required>
      </label>
      <label>Publishing Date
        <input type="date" name="date" value="<%= typeof date !== 'undefined' ? date : '' %>" required>
      </label>
      <label>Content
        <textarea name="content" required><%= typeof content !== 'undefined' ? content : '' %></textarea>
      </label>
      <button type="submit">Publish</button>
    </form>
    <a href="/admin">← Back to Dashboard</a>
  </div>
</body>
</html>
