<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Article – Admin</title>
  <style>
    body { font-family: 'Comic Neue', Arial, sans-serif; background: #fafafa; }
    .card { border: 2px solid #444; border-radius: 10px; padding: 2em; margin: 2em auto; max-width: 450px;}
    h2 { margin-top: 0; }
    label { display: block; margin-top: 1em; }
    input, textarea { width: 100%; font-size: 1em; border-radius: 5px; border: 1px solid #aaa; padding: 0.4em;}
    textarea { min-height: 120px; }
    button { margin-top: 1em; padding: 0.6em 1.2em; background: #444; color: #fff; border: none; border-radius: 6px; font-weight: bold;}
  </style>
</head>
<body>
  <div class="card">
    <h2>Update Article</h2>
    <form method="POST" action="/admin/edit/<%= article.slug %>">
      <label>Article Title
        <input type="text" name="title" value="<%= article.title %>" required>
      </label>
      <label>Publishing Date
        <input type="date" name="date" value="<%= article.date %>" required>
      </label>
      <label>Content
        <textarea name="content" required><%= content %></textarea>
      </label>
      <button type="submit">Update</button>
    </form>
    <a href="/admin">← Back to Dashboard</a>
  </div>
</body>
</html>
